import { Screen, Sound, ScreenContent } from "@/components";

const Home = () => {
  return (
    <div>
      <Screen>
        <ScreenContent />
      </Screen>
      <Sound />
    </div>
  );
};

export default Home;
