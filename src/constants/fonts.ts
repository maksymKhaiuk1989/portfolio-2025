import { Anonymous_Pro } from "next/font/google";

export const FONT_ANONYMOUS = Anonymous_Pro({
  variable: "--font-anonymous-pro",
  subsets: ["latin"],
  weight: ["400", "700"],
  display: "swap",
  preload: true,
});
